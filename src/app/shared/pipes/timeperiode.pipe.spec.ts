import { TimeperiodePipe } from './timeperiode.pipe';

describe('TimeperiodePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeperiodePipe();
    expect(pipe).toBeTruthy();
  });
});
